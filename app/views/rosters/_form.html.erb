<%= form_with(model: roster) do |form| %>
  <% if roster.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(roster.errors.count, "error") %> prohibited this roster from being saved:</h2>

      <ul>
        <% roster.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="row g-3">
    <div class="mb3">
      <%= form.label :name, class: "col-sm-1 col-form-label" %>
      <div class="col-sm-5">
        <%= form.text_field :name, class: "form-control" %>
      </div>
    </div>

    <div class="mb3">
      <%= form.label :faction, class: "col-sm-1 col-form-label" %>
      <div class="col-sm-5">
        <%= form.text_field :faction, class: "form-control" %>
      </div>
    </div>

    <div class="col-12">
      <%= form.submit class: "btn btn-primary" %>
    </div>
  </div>
<% end %>
